let valores = [ 5, 7, 9, 10, 11, ]
valores.length
valores.sort()

for ( let pos in valores ){                                      
    console.log(` A posiçao ${pos} tem o valor ${valores[pos]} `)
}

/*codigo simplificado de vetores onde for se le para e o in dentro ou seja For (Para) let pos(cada posiçao) in(dentro) Num (variavel) 
"para cada posiçao dentro de num (variavel)" console.log (num[pos]) eu vou mostrar o num pos */

